# Q01 — Initial pipeline spec dump (10 subsystems)

## Verbatim user input

Ten Claude Code-style prompts, one per subsystem:
1. Build the Scryfall API service
2. Build the card image retrieval pipeline
3. Build the Magic format history research schema
4. Build the web research ingestion module
5. Build the decklist/tournament evidence parser
6. Build the thematic card discovery engine
7. Build the card evaluation essay generator
8. Build the slideshow generator
9. Build the review dashboard
10. Build the orchestration CLI

Each was a substantial spec block (database tables, output formats, CLI commands).

## How I interpreted it

Per the workspace `CLAUDE.md` planning protocol, ten subsystems is project-scale work, not session-scale. Did not start coding. Asked the user whether to (a) run `/plan-joe-chip-scope` first, (b) override and start building, or (c) something else.

## What I built

Nothing yet — held the line on planning discipline.

## What I deferred

All 10 subsystems pending scope freeze and slice selection.
