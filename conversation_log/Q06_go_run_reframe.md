# Q06 — "go" (authorize reframe analysis)

## Verbatim user input

> go

## How I interpreted it

Authorization to run `/plan-mercer-reframe` on the topic-first hypothesis from Q03.

## What I built

A long reframe document delivered inline in the conversation, structured as the skill template prescribes:
- **WHAT YOU SAID** — the user's "Topic = any research target" framing
- **WHAT THAT IMPLIES** — one schema, one review UI, one packet, one workflow
- **WHAT'S ACTUALLY HAPPENING** — four genuinely different research-object types: themes, archetype-studies, format-questions, saved-queries — with different truth conditions, decay behavior, and reusability
- **THE GAP** — schema bloat, uneven reusability promise, saved-queries aren't research artifacts at all
- **BETTER FRAMING** — "persistent reusable research collections" as a small interface implemented by 3–4 distinct types, sharing review UI and packet protocol but not schema or evidence-scoring rules
- **WHAT THIS MEANS FOR IMPLEMENTATION** — slice 1 should pick ONE type (Theme, the most reusable), defer the others until prerequisites exist; saved-queries probably aren't a stored object at all

Closed by asking whether the reframing matched intent or whether I was missing something.

## What I deferred

Building anything — this was an analytic pass, output was the recommendation document.
