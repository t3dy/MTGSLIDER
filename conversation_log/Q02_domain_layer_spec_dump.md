# Q02 — Domain knowledge layer (10 more subsystems)

## Verbatim user input

Ten more spec blocks: format/era taxonomy, banned/restricted tracker, archetype ontology, claim conflict detector, source credibility scoring, card-printing timeline service, legality validator, art/theme annotation, card cluster/comparison engine, static website exporter.

## How I interpreted it

Second spec dump in a row. Now ~20 subsystems total with no scoping pass between them. Recognised this as the "let me also add..." sprawl pattern that the workspace `CLAUDE.md` explicitly warns about. Held the line again — pointed out the doubling and re-offered planning.

## What I built

Nothing — same hold as Q01.

## What I deferred

All 10 new subsystems. Noted that several depend on the foundation from Q01 and that the foundation didn't exist yet either.
